{
  "scripts": {
    "dev": "parcel src/index.html",
    "presamples2":
      "node -e 'console.log(`// THIS FILE IS AUTOGENERATED BY presamples SCRIPT\\nimport { _addSampleFilename } from \"./\";\\n${process.argv.slice(1).map(s => `\"../.${s}\"`).map(s => `require(${s});\\n_addSampleFilename(${s});`).join(\"\\n\")}`)' $(find ./src -name '*_sample.ts') > ./src/samples/imports.ts",
    "samples": "parcel src/samples/index.html",
    "test":
      "TS_NODE_COMPILER_OPTIONS='{\"module\":\"commonjs\"}' ts-node --eval 'declare const process:any;declare const require:any;process.argv.slice(4).forEach((s:any)=>require(s))' $(find ./src -name '*_test.ts') | tap-summary",
    "bench": "TS_NODE_COMPILER_OPTIONS='{\"module\":\"commonjs\"}' ts-node benchmark/index.ts",
    "build": "parcel build src/index.html --public-url ./ --out-dir www/",
    "updatesite": "rm -rf docs/app/* docs/samples/* && yarn run build && cp -r www/* docs/app/ && parcel build src/samples/index.html --public-url ./ --out-dir docs/samples/"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "benchmark": "^2.1.4",
    "node-sass": "^4.7.2",
    "parcel-bundler": "^1.1.0",
    "tap-summary": "^4.0.0",
    "tape": "^4.8.0",
    "ts-node": "^4.0.2",
    "typescript": "^2.6.2"
  },
  "dependencies": {
    "classcat": "^1.1.1",
    "icepick": "^2.2.1",
    "picodom": "^1.0.2",
    "tslib": "^1.8.1"
  }
}
